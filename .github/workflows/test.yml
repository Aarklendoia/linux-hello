name: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-26.04
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libssl-dev \
            libpam0g-dev \
            libv4l-dev \
            libjpeg-dev \
            libclang-dev \
            clang \
            pkg-config \
            dbus \
            libonnxruntime1.23
      
      - name: Cache cargo registry
        uses: actions/cache@v5
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache cargo index
        uses: actions/cache@v5
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Cache cargo build
        uses: actions/cache@v5
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}
      
      - name: Build release binaries (télécharge les modèles ONNX via build.rs)
        env:
          ORT_DYLIB_PATH: /usr/lib/x86_64-linux-gnu/libonnxruntime.so.1.23
        run: |
          cd $GITHUB_WORKSPACE
          cargo build --all --release
      
      - name: Run tests
        env:
          ORT_DYLIB_PATH: /usr/lib/x86_64-linux-gnu/libonnxruntime.so.1.23
        run: |
          cd $GITHUB_WORKSPACE
          cargo test --all --release
      
      - name: Run clippy
        run: |
          cd $GITHUB_WORKSPACE
          cargo clippy --all --release -- -D warnings || true
